{
  "name": "legal-skills-monorepo",
  "private": true,
  "description": "Legal Skills directory: a solarpunk registry of legal skills, agents, and MCPs.",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "test": "npm run test --prefix web",
    "preview": "npm run preview --prefix web",
    "web:dev": "npm run dev --prefix web",
    "web:build": "npm run build --prefix web",
    "web:test": "npm run test --prefix web",
    "web:preview": "npm run preview --prefix web"
  }
}
